[project]
name = "Qtcord"
version = "0.1.0"
description = "PySide Discord client"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "keyring~=25.7",
    "platformdirs~=4.10",
    "pyside6~=6.11",
    "requests~=2.34",
    "xxhash~=3.7",
]
