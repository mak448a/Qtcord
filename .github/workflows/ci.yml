name: CI Build & Release

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main ]

# This workflow builds and lints the application.
# It uses the built-in GITHUB_TOKEN to release and DOES NOT require any external secrets.

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Ruff
        run: pip install ruff

      - name: Ruff Check
        run: ruff check src/

      - name: Ruff Format Check
        run: ruff format --check src/

  build-windows:
    name: Build (Windows)
    needs: lint
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Build
        run: python helper_scripts/package_windows.py

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Qtcord-Windows
          path: src/dist/Qtcord-Windows
          if-no-files-found: error

  build-macos:
    name: Build (macOS)
    needs: lint
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Build
        run: python3 helper_scripts/package_mac.py

      - name: Prepare artifact
        run: |
          mkdir -p src/dist/Qtcord-macOS
          mv src/dist/Qtcord-macOS.app src/dist/Qtcord-macOS/

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Qtcord-macOS
          path: src/dist/Qtcord-macOS
          if-no-files-found: error

  build-linux:
    name: Build (Linux)
    needs: lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt pyinstaller

      - name: Build
        working-directory: src
        run: pyinstaller ../helper_scripts/linux.spec

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Qtcord-Linux
          path: src/dist/Qtcord-Linux
          if-no-files-found: error

  release:
    name: Create Release
    needs: [build-windows, build-macos, build-linux]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Package release assets
        run: |
          cd artifacts
          zip -r ../Qtcord-Windows.zip Qtcord-Windows/
          zip -r ../Qtcord-macOS.zip Qtcord-macOS/
          cd ..
          cp artifacts/Qtcord-Linux/Qtcord-Linux .
          chmod +x Qtcord-Linux

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            Qtcord-Windows.zip
            Qtcord-macOS.zip
            Qtcord-Linux
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
